import 'package:flutter/material.dart';

class DetailTrackingHistory extends StatelessWidget {
  final String id;
  const DetailTrackingHistory({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return Text('Detail Tracking History');
  }
}
